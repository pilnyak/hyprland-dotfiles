# Waybar-only wallust config
# Uses cropped top portion of wallpaper for better color matching

backend = "fastresize"
color_space = "lab"
palette = "harddark"
check_contrast = true

[templates]
waybar = { src = 'waybar.css', dst = '~/.config/waybar/colors.css' }
